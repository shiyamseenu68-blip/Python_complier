import {useStore} from '../store';import {Play,Square,Settings,PanelLeft,Command,Download,ZoomIn,ZoomOut,PanelBottom,Code2,Clock} from 'lucide-react';import {motion} from 'framer-motion';
export function TopBar({onRun,onStop}:{onRun():void;onStop():void}){const{running,toggleSidebar,setShowCmd,setShowCfg,setShowExport,termOpen,setTermOpen,activeFile,zoomIn,zoomOut,cfg,setShowHistory,setShowSnippets}=useStore();const file=activeFile();
return(<div className="flex h-11 shrink-0 items-center justify-between border-b border-cv-border bg-cv-bg px-3"><div className="flex items-center gap-2"><button onClick={toggleSidebar} className="ib"><PanelLeft size={15}/></button><div className="flex items-center gap-1.5"><div className="flex h-6 w-6 items-center justify-center rounded-lg text-[10px] font-black text-white" style={{background:`linear-gradient(135deg,${cfg.accent},#6366f1)`}}>PY</div><span className="hidden text-[13px] font-semibold text-cv-text sm:inline">Python Studio</span></div>{file&&<span className="hidden text-xs text-cv-muted md:inline">/ {file.name}</span>}</div>
<button onClick={()=>setShowCmd(true)} className="hidden items-center gap-2 rounded-lg border border-cv-border bg-cv-surface/80 px-3 py-1 text-xs text-cv-muted hover:bg-cv-elevated hover:text-cv-text sm:flex"><Command size={11}/> Commands <kbd className="rounded bg-cv-elevated px-1 py-0.5 text-[9px]">⌘K</kbd></button>
<div className="flex items-center gap-1"><div className="hidden items-center rounded-lg border border-cv-border bg-cv-surface/80 md:flex"><button onClick={zoomOut} className="ib !px-2 !py-1.5"><ZoomOut size={12}/></button><button onClick={zoomIn} className="ib !px-2 !py-1.5"><ZoomIn size={12}/></button></div><button onClick={()=>setShowSnippets(true)} className="ib hidden md:flex"><Code2 size={14}/></button><button onClick={()=>setShowHistory(true)} className="ib hidden md:flex"><Clock size={14}/></button><button onClick={()=>setTermOpen(!termOpen)} className="ib hidden md:flex"><PanelBottom size={14}/></button><button onClick={()=>setShowExport(true)} className="ib hidden sm:flex"><Download size={14}/></button><button onClick={()=>setShowCfg(true)} className="ib"><Settings size={14}/></button>
{running?<motion.button whileTap={{scale:0.92}} onClick={onStop} className="flex items-center gap-1 rounded-lg bg-cv-error px-3 py-1.5 text-xs font-semibold text-white hover:opacity-90"><Square size={11} fill="currentColor"/> Stop</motion.button>:<motion.button whileTap={{scale:0.92}} onClick={onRun} className="flex items-center gap-1 rounded-lg bg-cv-success px-3 py-1.5 text-xs font-semibold text-white hover:opacity-90"><Play size={11} fill="currentColor"/> Run</motion.button>}</div></div>);}
