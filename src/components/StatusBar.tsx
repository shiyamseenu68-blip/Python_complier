import {useStore} from '../store';
export function StatusBar(){const{activeFile,cfg,running,files}=useStore();const f=activeFile(),dirty=files.filter(x=>x.isDirty).length;return(<div className="flex h-6 shrink-0 items-center justify-between bg-cv-accent px-3 text-[10px] font-medium text-white/90"><div className="flex items-center gap-3">{running&&<span className="flex items-center gap-1"><span className="h-1.5 w-1.5 animate-pulse rounded-full bg-white"/>Running…</span>}{!running&&f&&<span>{f.name}</span>}{f&&<span className="opacity-70">{(f.language??'txt').toUpperCase()}</span>}</div><div className="flex items-center gap-3 opacity-75">{dirty>0&&<span>{dirty} unsaved</span>}<span>{cfg.fontSize}px</span></div></div>);}
